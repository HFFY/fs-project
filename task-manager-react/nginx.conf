server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # La app usa BrowserRouter: cualquier ruta que no sea un archivo real
    # (ej. /login al recargar) debe devolver index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
