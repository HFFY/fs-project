# Plantilla para correr el backend en local (npm run dev / prisma).
# Copiala a backend/.env y completa los valores. Nunca subas el .env real.
DATABASE_URL=postgresql://appuser:cambia_esta_contrasena@localhost:5432/appdb

# Genera uno con: openssl rand -hex 32
JWT_SECRET=cambia_este_secreto

PORT=3000
NODE_ENV=development
